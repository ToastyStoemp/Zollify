/** HS code + country tables — verbatim from the legacy customs tool (www/app.js). */

export { HS_CODES, type HsCode } from '@zollify/shared';
export { COUNTRIES, COUNTRY_BY_CODE, COUNTRY_CODES } from '@zollify/shared';
